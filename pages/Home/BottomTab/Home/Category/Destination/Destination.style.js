import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        paddingVertical: 40
    },
    map: {
        width: '100%',
        height: '100%'
    }
})

export default styles