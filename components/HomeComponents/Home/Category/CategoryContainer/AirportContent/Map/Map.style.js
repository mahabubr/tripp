import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        height: '100%',
    },
    marker_img: {
        width: 50,
        height: 50,
        resizeMode: 'contain'
    }
})

export default styles